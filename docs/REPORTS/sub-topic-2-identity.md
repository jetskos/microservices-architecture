# Sub-topic 2: Security & Identity Provider

Implemented JWT-based centralized authentication with bcrypt password hashing and RBAC roles (`patient`, `dentist`, `admin`). Unauthorized access and invalid credentials are logged as security events.

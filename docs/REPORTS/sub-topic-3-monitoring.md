# Sub-topic 3: Observability & Monitoring

Each service exposes Prometheus `/metrics` for request count, latency, and errors. Grafana dashboard JSON files provide service health, authentication failures, and error indicators. Alerts are configured for high error rates and slow responses.
